#!/usr/bin/python
from sys import argv, exit, stdout
import ContentGen as cg

if __name__ == '__main__':
	exit(0)

gen.addNodes([
DocStart(),
PageTitle('Test Page'),
PageSection('Test Section'),
DateString(),


DocEnd()
])
